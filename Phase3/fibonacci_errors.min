function fibonacci;
beginparams
	k : integer;
endparams
beginlocals
endlocals
beginbody
	continue;
	if (k <= 1) then return 1; endif;
	return fibonacci(k - 1) + fibonacci(k - 2);
endbody

function meeps;
beginparams
endparams
beginlocals
	n : integer;
	n : integer;
	t : array [20] of integer;
	he : array [0] of integer;
	fib_n : integer;
endlocals
beginbody
	read n;
	z := n;
	fib_n[3] := fib_n;
	t := n;
	n := THIS_WONT_RUN(n);
	fib_n := fibonacci(n);
	write fib_n;
endbody
